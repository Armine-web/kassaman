import type { CSSProperties } from 'react';
export const sliderStyles = {
  track: {
    backgroundColor: '#c5a059',
  } as CSSProperties,

  handle: {
    borderColor: '#c5a059',
    backgroundColor: '#fff',
    boxShadow: 'none',
  } as CSSProperties,
};
